const{Router:Router}=require("express"),controladorAuth=require("../controladores/auth.controlador"),verificarToken=require("../middlewares/verificarToken"),router=Router();router.post("/login",controladorAuth.login),router.get("/perfil",verificarToken,controladorAuth.getPerfil),module.exports=router;
const{Router:Router}=require("express"),controlador=require("../controladores/areas.controlador"),verificarToken=require("../middlewares/verificarToken"),router=Router();router.use(verificarToken),router.get("/",controlador.getAreas),module.exports=router;